---
"@hadl-labs/changelog-github": minor
---

Replace `clickup:` field with generic `card:` field that accepts any URL. Supports Trello, Linear, and any other card management tool. CHANGELOG output label updated from `ClickUp:` to `Card:`.
